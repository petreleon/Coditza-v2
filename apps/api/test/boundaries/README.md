# Boundary fixture location

`ARC-BOUND-001` owns the enforcement tool, positive controls, and every
negative import fixture. FOUND-001 reserves only this tracked location; it does
not introduce a module directory, an import rule, or a runnable boundary test.
