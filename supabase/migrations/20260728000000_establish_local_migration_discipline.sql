-- SUP-LOCAL-002 establishes the migration/reset workflow only.
-- Domain schemas, roles, RLS, Auth data, and curriculum data remain owned by
-- their named later tasks. This intentionally has no application schema effect.
SELECT 1;
